# ローカル環境用のDB設定
DB_HOST=db
DB_NAME=myapp_development
DB_USERNAME=root
DB_PASSWORD=password
DB_PORT=3306

# 外部連携（OAuth/Tweet）のクライアント情報
# 初期状態では誤動作防止のためコメントアウトしています
# 実際にOAuth連携を試す場合は、コメントアウトを外して適切な値を設定してください
# WARNING: ダブルクォーテーション付きの項目は、必ず文字列のまま指定する

# OAUTH_AUTHORIZE_URL=http://localhost:PORT/oauth/authorize
# OAUTH_TOKEN_URL=http://localhost:PORT/oauth/token
# OAUTH_CLIENT_ID="your_client_id_here"
# OAUTH_CLIENT_SECRET="your_client_secret_here"
# OAUTH_REDIRECT_URI=http://localhost:3000/oauth/callback
# OAUTH_SCOPE=read

# TWEET_CREATE_URL=http://localhost:PORT/api/tweets